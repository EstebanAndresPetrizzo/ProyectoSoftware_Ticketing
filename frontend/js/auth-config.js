/**
 * Opcional: si lo dejas vacío, el login lee el Client ID desde la API o, en Docker
 * con Ticketing__EnableDockerDevLogin, puedes usar solo "Modo prueba (sin Google)".
 *
 * Mismo valor que Authentication:Google:ClientId en la API.
 */
export const GOOGLE_CLIENT_ID = "";
