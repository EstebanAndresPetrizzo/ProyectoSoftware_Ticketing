# Copia este archivo a ".env" en la misma carpeta que docker-compose.yml (el .env no se sube a git).
# Opcional: si lo defines, aparecerá también el botón "Continuar con Google".
# GOOGLE_CLIENT_ID=tu-client-id.apps.googleusercontent.com
